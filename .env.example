# Required — get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_key_here

# Optional — STT runs locally via whisper.cpp (no key needed).
# Override the model size if desired: tiny.en | base.en | small.en | medium.en
# WHISPER_MODEL=base.en
